import { App } from "./app";
import { clientBootstrap } from "./bootstrap/bootstrap.browser";

// ReactDOM.render(React.createElement(App), document.getElementById("root"));

clientBootstrap(App);

